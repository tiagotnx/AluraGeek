import styled from 'styled-components'

export const Main = styled.main`
    display: flex;
    flex-direction: column;
    width: calc(100% - 4rem);
    max-width: 70rem;
    margin: 0 auto;
    gap: 4rem;
    padding: 4rem 0;
`
    